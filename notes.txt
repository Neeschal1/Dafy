uvicorn config.asgi:application --reload
docker build -t neeschal1/dafy .
first_name is the fullname of my user model

Swagger setups:

# from rest_framework.permissions import IsAdminUser
# from drf_yasg.views import get_schema_view
# from drf_yasg import openapi
# from rest_framework.authentication import SessionAuthentication, BasicAuthentication

# schema_view = get_schema_view(
#     openapi.Info(
#         title="Dafy Backend API Docs",
#         default_version="v1",
#         description="API documentation for Dafy, an e-commerce mobile application.",
#         public = True,
#         permission_classes = (IsAdminUser),
#         authentication_classes = (SessionAuthentication, BasicAuthentication)
#     )
# )

# path('docs/', schema_view.with_ui('swagger', cache_timeout=0), name='Swagger View'),
